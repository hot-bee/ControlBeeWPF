<UserControl x:Class="ControlBeeWPF.Views.RecipeManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ControlBeeWPF.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:RecipeManagerViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.Resources>
            <Style TargetType="Button" BasedOn="{StaticResource ExtrudedButtonWordWrapStyle}">
                <Setter Property="Width" Value="120" />
                <Setter Property="Height" Value="40" />
                <Setter Property="Margin" Value="0 10" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <GroupBox Grid.Column="0" Header="Recipe List" Padding="5">
            <ListBox Name="RecipeListBox"
                     ItemsSource="{Binding LocalNames}" />
        </GroupBox>
        <StackPanel Grid.Column="1">
            <GroupBox Header="Current Recipe" Margin="5">
                <TextBox Name="RecipeName" 
                         Margin="5 10"
                         Text="{Binding LocalName}" 
                         Width="120"/>
            </GroupBox>
            <GroupBox Header="Actions" Margin="5">
                <StackPanel>
                    <Button Name="OpenButton" Content="Open" Click="OpenButton_OnClick" />
                    <Button Name="SaveAsButton" Content="SaveAs" Click="SaveAsButton_OnClick" />
                    <Button Name="DeleteButton" Content="Delete" Click="DeleteButton_OnClick" />
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>