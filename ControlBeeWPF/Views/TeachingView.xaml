<UserControl
    x:Class="ControlBeeWPF.Views.TeachingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:ControlBeeWPF.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:TeachingViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <GroupBox
            Grid.Column="0"
            Margin="5"
            Header="Positions">
            <ScrollViewer Margin="5" VerticalScrollBarVisibility="Auto">
                <ListBox Name="PositionItemList" SelectionChanged="PositionItemList_OnSelectionChanged" />
            </ScrollViewer>
        </GroupBox>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <GroupBox
                Grid.Row="0"
                Margin="5"
                Header="Data">
                <ContentControl Name="DataContent" Margin="5" />
            </GroupBox>
            <GroupBox
                Grid.Row="1"
                Margin="5"
                Header="Axis Status">
                <ContentControl Name="AxisStatusContent" Margin="5" />
            </GroupBox>
        </Grid>

        <GroupBox
            Grid.Column="2"
            Margin="5"
            Header="Jog">
            <ContentControl Name="JogContent" Margin="5" />
        </GroupBox>
    </Grid>
</UserControl>