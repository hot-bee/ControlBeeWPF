<UserControl x:Class="ControlBeeWPF.Views.EditableFrameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Border BorderBrush="#CCC" BorderThickness="1" Background="White" Padding="10">
        <DockPanel>
            <!-- Toolbar / header -->
            <StackPanel Name="ControlPanel" Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,10" HorizontalAlignment="Right">
                <!-- Save -->
                <Button Content="💾 Save"
                        Margin="0,0,5,0"
                        Padding="6,2"
                        Command="{Binding SaveCommand}"
                        IsEnabled="{Binding Modified}"
                        Background="#E0FFE0"
                        BorderBrush="#80C080" />

                <!-- Discard -->
                <Button Content="❌ Discard"
                        Padding="6,2"
                        Command="{Binding DiscardCommand}"
                        IsEnabled="{Binding Modified}"
                        Background="#FFE0E0"
                        BorderBrush="#C08080" />
            </StackPanel>

            <!-- Content area (whatever user places inside) -->
            <ContentControl Name="MyContent" />
        </DockPanel>
    </Border>
</UserControl>