<UserControl
    x:Class="ControlBeeWPF.Views.TeachingJogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:ControlBeeWPF.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewModels:TeachingJogViewModel}"
    d:DesignHeight="650"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <Style BasedOn="{StaticResource ExtrudedButtonWordWrapStyle}" TargetType="Button">
                <Setter Property="Width" Value="120" />
                <Setter Property="Height" Value="50" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Header="Move to Position">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <Button
                    Margin="10"
                    Command="{Binding MoveToHomePosCommand}"
                    Content="Move to Home" />
                <Rectangle Width="60" />
                <Button
                    Margin="10"
                    Command="{Binding MoveToSavedPosCommand}"
                    Content="Move to Saved &#x0a;Position" />
            </StackPanel>
        </GroupBox>
        <GroupBox
            Grid.Row="1"
            Margin="0,5,0,0"
            Header="Jog Control">
            <ContentControl Name="JogControlArea" />
        </GroupBox>
        <GroupBox
            Grid.Row="2"
            Margin="0,5,0,0"
            Header="Position Saving">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <Button
                    Margin="10"
                    Command="{Binding SetPosCommand}"
                    Content="Set" />
                <Rectangle Width="80" />
                <Button
                    Margin="10"
                    Content="Restore"
                    IsEnabled="False" />
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>