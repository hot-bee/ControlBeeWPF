<UserControl
    x:Class="ControlBeeWPF.Views.IoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:ControlBeeWPF.Views"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="{x:Type GroupBox}">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="Silver" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Margin" Value="5" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Border
                                    Grid.Row="0"
                                    Padding="5"
                                    Background="#F5F5F5"
                                    CornerRadius="15,15,0,0">
                                    <Label
                                        Height="30"
                                        VerticalContentAlignment="Center"
                                        FontSize="15">
                                        <ContentPresenter ContentSource="Header" />
                                    </Label>
                                </Border>
                                <Border Grid.Row="1" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter />
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="views:DigitalInputStatusBarView">
            <Setter Property="NameColumnWidth" Value="200" />
        </Style>

        <Style TargetType="views:DigitalOutputStatusBarView">
            <Setter Property="NameColumnWidth" Value="200" />
        </Style>

        <Style x:Key="PagerButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Width" Value="50" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            x:Key="PagerButtonActiveStyle"
            BasedOn="{StaticResource PagerButtonStyle}"
            TargetType="Button">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Background" Value="LightGoldenrodYellow" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Input">
                <DockPanel LastChildFill="True">
                    <DockPanel Margin="0,10,0,0" DockPanel.Dock="Bottom">
                        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">

                            <Button
                                Name="InputFirstButton"
                                Content="&lt;&lt;"
                                Style="{StaticResource PagerButtonStyle}" />

                            <Button
                                Name="InputPrevButton"
                                Content="&lt;"
                                Style="{StaticResource PagerButtonStyle}" />

                            <WrapPanel
                                Name="InputPager"
                                Margin="5,0"
                                VerticalAlignment="Center" />

                            <Button
                                Name="InputNextButton"
                                Content="&gt;"
                                Style="{StaticResource PagerButtonStyle}" />

                            <Button
                                Name="InputLastButton"
                                Content="&gt;&gt;"
                                Style="{StaticResource PagerButtonStyle}" />
                        </StackPanel>
                    </DockPanel>
                    <Grid Name="InputGrid" />
                </DockPanel>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Output">
                <DockPanel LastChildFill="True">
                    <DockPanel Margin="0,10,0,0" DockPanel.Dock="Bottom">
                        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">

                            <Button
                                Name="OutputFirstButton"
                                Content="&lt;&lt;"
                                Style="{StaticResource PagerButtonStyle}" />

                            <Button
                                Name="OutputPrevButton"
                                Content="&lt;"
                                Style="{StaticResource PagerButtonStyle}" />

                            <WrapPanel
                                Name="OutputPager"
                                Margin="5,0"
                                VerticalAlignment="Center" />

                            <Button
                                Name="OutputNextButton"
                                Content="&gt;"
                                Style="{StaticResource PagerButtonStyle}" />

                            <Button
                                Name="OutputLastButton"
                                Content="&gt;&gt;"
                                Style="{StaticResource PagerButtonStyle}" />
                        </StackPanel>
                    </DockPanel>
                    <Grid Name="OutputGrid" />
                </DockPanel>
            </GroupBox>
        </Grid>
    </ScrollViewer>
</UserControl>