<UserControl
    x:Class="ControlBeeWPF.Views.ActorItemExplorerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:ControlBeeWPF.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewModels:ActorItemExplorerViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <GroupBox
            Grid.Column="0"
            Margin="5"
            Header="Items">
            <TreeView
                Margin="5"
                DataContext="{Binding ActorItemTreeViewModel}"
                ItemsSource="{Binding ActorItemTreeCollection.Root.Children}"
                SelectedItemChanged="TreeView_OnSelectedItemChanged">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type viewModels:ActorItemTreeNode}" ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Data.Title}" />
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </GroupBox>
        <GroupBox
            Grid.Column="1"
            Margin="5"
            Header="Details">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Label Name="MyContentLabel" Grid.Row="0" />
                    <ContentControl Name="MyContentControl" Grid.Row="1" />
                </Grid>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>