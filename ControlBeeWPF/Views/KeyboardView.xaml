<Window x:Class="ControlBeeWPF.Views.KeyboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:ControlBeeWPF.ViewModels"
        mc:Ignorable="d"
        Title="Keyboard" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="Window_PreviewKeyDown"
        FocusManager.FocusedElement="{Binding RelativeSource={RelativeSource Self}}">

    <d:Window.DataContext>
        <d:DesignInstance Type="viewModels:KeyboardViewModel" />
    </d:Window.DataContext>

    <Grid Margin="15" Focusable="True">
        <Grid.Resources>
            <Style TargetType="Control" x:Key="ControlStyle">
                <Setter Property="FontSize" Value="16" />
            </Style>

            <Style TargetType="Button" BasedOn="{StaticResource ControlStyle}">
                <Setter Property="Height" Value="30" />
                <Setter Property="Background" Value="WhiteSmoke" />
                <Setter Property="Margin" Value="10" />
            </Style>

            <Style TargetType="TextBox" BasedOn="{StaticResource ControlStyle}">
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Padding" Value="10 0 10 0" />
                <Setter Property="Background" Value="FloralWhite" />
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Column="0" Grid.Row="0"
                 Name="ValueText"
                 Height="40"
                 Text="{Binding Input, Mode=OneWay}"
                 Background="White"
                 BorderBrush="Black"
                 BorderThickness="1"
                 IsReadOnly="True" 
                 Focusable="False" 
                 IsTabStop="False"
                 Margin="0,8,0,7" />

        <Button Grid.Column="1" Grid.Row="0" 
                Content="Clear"
                Name="ClearButton"
                Command="{Binding ClearCommand}"/>

        <Button Grid.Column="1" Grid.Row="1"
                Content="Back"
                Name="BackButton"
                Command="{Binding BackspaceCommand}"/>

        <Border Grid.Column="0" Grid.Row="1" Grid.RowSpan="4" BorderBrush="Silver" BorderThickness="1" Padding="5" Margin="5,5,5,5">
            <ItemsControl ItemsSource="{Binding Keys}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="8"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding}"
                                Command="{Binding DataContext.InputLetterCommand,
                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                FontWeight="SemiBold" Height="45" Width="50" Margin="10" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <Button Grid.Column="1" Grid.Row ="3"
                Content="Cancel"
                Name="CancelButton"
                Click="Button_OnClick"/>

        <Button Grid.Column="1" Grid.Row ="4"
                Content="Enter"
                Name="EnterButton"
                Click="Button_OnClick"/>
    </Grid>
</Window>
